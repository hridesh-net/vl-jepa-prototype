[project]
name = "vl-jepa"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "torch",
  "torchvision",
  "transformers",
  "sentence-transformers",
  "einops",
  "numpy",
  "pillow",
  "tqdm",
  "opencv-python"
]
